import {useSate,useEffect} from "react";

export default function Users() {
    const getData= fetch(api).then((res)=>res.json());
    const fetchandGetUser
    return ( <div>
        <h1>Users Page</h1>
    </div>
    );
}
